version: '3.7'
services:
  # Web server
  nginx:
    container_name: nginx
    image: nginx:1.21
    ports: 
    - "443:443"
    volumes:
      # - ~/data/wp:/var/www/wordpress
      - ./html:/usr/share/nginx/html:ro
      #   # primeiro é o volume que sera montato
      #   # segundo é onde sera montado
      #   # terceiro sao as permissoes
      - ./nginx:/etc/nginx
      - ./cert:/cert
    # restart: always
    # networks:
    #   - wordpress_network
 




  # Wordpress
  # wordpress:
  #   container_name: wordpress
  #   build: requirements/php
  #   ports:
  #     - "9000:9000"
  #   restart: always
  #   # networks:
  #   #   - wordpress_network
  #   volumes:
  #     - ~/data/wp:/var/www/wordpress


# networks:
#   wordpress_network:
  
# volumes:
#   wp:
#     driver_opts:
#       type: volume
#       device: /home/gabriel/Projetos/Inception/data/wordpress